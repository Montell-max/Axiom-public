[workspace]
members = [
    "axiom-core",
    "axiom-integrity",
    "axiom-identity",
    "axiom-state",
    "axiom-boundary",
    "axiom-sanctuary",
    "axiom-demo",
]
# Shared deps if needed, but minimal
