[package]
name = "axiom-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Minimal crypto deps, e.g., sha2 for hashing, ed25519 for sigs
sha2 = "0.10"
ed25519-dalek = { version = "2.0", features = ["std"] }
rand = "0.9.2"
